version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema"
    queries: "sql/queries"
    gen:
      go:
        out: "internal/database"
    #     emit_json_tags: true
    #     emit_prepared_queries: false
    #     emit_interface: true
    #     emit_exact_table_names: false
    #     emit_empty_slices: true
    # database:
    #   uri: "postgres://root:123@localhost:5432/gorssdb?sslmode=disable"